venv/
__pycache__/
*.pyc
*.pyo
*.log
.env
.DS_Store
logs/*
models/
*.html.tmp
.pytest_cache/
.vercel/
.claude/
scenarios/results/*.json
scenarios/results/*.jsonl
!scenarios/results/REPORT.md
.vercel
sweep_out/
# Working files from live tests and analysis (Rajat 2026-09-11: everything stays
# inside warp-av, never on the desktop). Not part of the project.
scratch/

# The brains and recordings that matter are kept in the repo (small): the
# rest of models/ and logs/ stay ignored.
!rl/models/
!rl/models/*.zip
!rl/models/README.md
!logs/demo_runs/
!logs/demo_runs/*.jsonl
